/* =========================
  DatoClaro - Styles
  Paleta:
  Azul:   #0A1F44
  Dorado: #C9A24D
  Blanco: #FFFFFF
  Beige:  #F9F5EF
========================= */

:root{
  --navy:#0A1F44;
  --gold:#C9A24D;
  --white:#FFFFFF;
  --beige:#F9F5EF;
  --text:#0b1220;
  --muted:rgba(11,18,32,.72);
  --line:rgba(10,31,68,.14);
  --shadow: 0 12px 30px rgba(10,31,68,.12);
  --radius: 18px;
}

*{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color:var(--text);
  background:var(--white);
  line-height:1.55;
}

img{ max-width:100%; display:block; }
a{ color:inherit; text-decoration:none; }
a:hover{ text-decoration:underline; }

.container{
  width:min(1120px, calc(100% - 40px));
  margin-inline:auto;
}

/* Header */
.header{
  position:sticky;
  top:0;
  z-index:50;
  background:rgba(255,255,255,.86);
  backdrop-filter:saturate(160%) blur(12px);
  border-bottom:1px solid var(--line);
}
.header__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:14px 0;
  gap:18px;
}

.brand{ display:flex; align-items:center; gap:10px; }
.brand__mark{
  width:38px; height:38px;
  display:grid; place-items:center;
  background:var(--navy);
  color:var(--gold);
  font-weight:700;
  border-radius:12px;
  box-shadow:0 10px 22px rgba(10,31,68,.18);
}
.brand__text{ font-weight:700; letter-spacing:.2px; }

.nav{ display:flex; align-items:center; gap:18px; }
.nav a{ font-weight:500; color:rgba(11,18,32,.88); }
.nav__cta{
  padding:10px 14px;
  border-radius:999px;
  border:1px solid rgba(201,162,77,.45);
  background:rgba(201,162,77,.10);
}
.nav__cta:hover{ text-decoration:none; background:rgba(201,162,77,.18); }

/* Burger (mobile) */
.burger{
  display:none;
  border:1px solid var(--line);
  background:transparent;
  border-radius:12px;
  padding:10px;
  cursor:pointer;
}
.burger span{
  display:block;
  width:22px; height:2px;
  background:var(--navy);
  margin:4px 0;
  border-radius:2px;
}
.mobile-nav{
  display:none;
  border-top:1px solid var(--line);
  padding:10px 20px 16px;
}
.mobile-nav a{
  display:block;
  padding:10px 0;
  border-bottom:1px solid rgba(10,31,68,.08);
}

/* Hero */
.hero{
  position:relative;
  overflow:hidden;
  border-bottom:1px solid var(--line);
}
.hero__bg{
  position:absolute; inset:0;
  background:
    linear-gradient(120deg, rgba(10,31,68,.92), rgba(10,31,68,.78)),
    url("../img/hero-abstract.svg") center/cover no-repeat;
  transform:scale(1.02);
}
.hero__content{
  position:relative;
  padding:76px 0 64px;
  color:var(--white);
}
.kicker{
  display:inline-block;
  padding:8px 12px;
  border-radius:999px;
  background:rgba(255,255,255,.10);
  border:1px solid rgba(255,255,255,.18);
  font-size:13px;
}
.hero h1{
  margin:14px 0 10px;
  font-size: clamp(30px, 4.2vw, 52px);
  line-height:1.1;
  letter-spacing:-.6px;
}
.lead{
  max-width:740px;
  font-size: clamp(16px, 1.6vw, 18px);
  color:rgba(255,255,255,.88);
}
.hero__actions{ display:flex; gap:12px; margin-top:22px; flex-wrap:wrap; }
.hero__meta{
  display:flex; gap:10px; flex-wrap:wrap;
  margin-top:26px;
}
.pill{
  padding:8px 12px;
  border-radius:999px;
  background:rgba(201,162,77,.14);
  border:1px solid rgba(201,162,77,.40);
  color:rgba(255,255,255,.92);
  font-size:13px;
}

/* Sections */
.section{ padding:70px 0; }
.section--soft{ background:var(--beige); border-top:1px solid rgba(10,31,68,.08); border-bottom:1px solid rgba(10,31,68,.08); }
.section__head{ max-width:760px; }
.section__head h2{ margin:0 0 10px; font-size: clamp(24px, 2.4vw, 34px); letter-spacing:-.3px; }
.section__head p{ margin:0; color:var(--muted); }

/* Cards / grids */
.grid-3{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:18px;
  margin-top:24px;
}
.card{
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:18px 18px 16px;
  background:var(--white);
  box-shadow:var(--shadow);
}
.card__icon{ font-size:22px; }
.card h3{ margin:10px 0 6px; }
.muted{ color:var(--muted); }
.small{ font-size:13px; }
.list{ margin:12px 0 0; padding-left:18px; color:rgba(11,18,32,.86); }
.note{
  margin-top:14px;
  padding:12px 12px;
  background:rgba(201,162,77,.10);
  border:1px solid rgba(201,162,77,.30);
  border-radius:14px;
}

/* Media cards */
.media-card{
  margin:0;
  border-radius:var(--radius);
  overflow:hidden;
  border:1px solid rgba(10,31,68,.10);
  background:var(--white);
  box-shadow:var(--shadow);
}
.media-card img{ width:100%; height:auto; }
.media-card figcaption{ padding:12px 14px 14px; color:rgba(11,18,32,.86); }
.footnote{ margin-top:18px; color:var(--muted); }

/* Pricing */
.pricing{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:18px;
  margin-top:24px;
}
.price-card{
  position:relative;
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:20px 18px 16px;
  background:var(--white);
  box-shadow:var(--shadow);
}
.price-card--featured{
  border-color:rgba(201,162,77,.55);
  box-shadow: 0 16px 40px rgba(10,31,68,.16);
}
.badge{
  position:absolute;
  top:14px; right:14px;
  font-size:12px;
  padding:6px 10px;
  border-radius:999px;
  background:rgba(201,162,77,.18);
  border:1px solid rgba(201,162,77,.35);
}
.price{ margin:10px 0 8px; }

/* Team */
.team{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:18px;
  margin-top:24px;
}
.team-card{
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:18px;
  background:var(--white);
  box-shadow:var(--shadow);
}

/* Contact */
.contact{
  display:grid;
  grid-template-columns: 1.6fr .9fr;
  gap:18px;
  margin-top:24px;
  align-items:start;
}
.form{
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:18px;
  background:var(--white);
  box-shadow:var(--shadow);
}
.field{ margin:12px 0; }
label{ display:block; font-weight:600; font-size:13px; margin-bottom:8px; }
input, select, textarea{
  width:100%;
  padding:12px 12px;
  border-radius:14px;
  border:1px solid rgba(10,31,68,.18);
  outline:none;
  font:inherit;
  background:var(--white);
}
input:focus, select:focus, textarea:focus{
  border-color: rgba(201,162,77,.75);
  box-shadow: 0 0 0 4px rgba(201,162,77,.14);
}
.grid-2{
  display:grid;
  grid-template-columns: repeat(2, 1fr);
  gap:12px;
}
.form__actions{
  display:flex;
  align-items:center;
  gap:12px;
  flex-wrap:wrap;
  margin-top:12px;
}
.form__status{ margin-top:12px; color:var(--muted); }

.contact-card{
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:18px;
  background:var(--white);
  box-shadow:var(--shadow);
}
.divider{ height:1px; background:rgba(10,31,68,.10); margin:16px 0; }
.quote{ color:rgba(11,18,32,.88); }

/* Buttons */
.btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  padding:12px 16px;
  border-radius:999px;
  border:1px solid transparent;
  cursor:pointer;
  font-weight:600;
  text-decoration:none !important;
}
.btn--primary{
  background:var(--gold);
  border-color:rgba(201,162,77,.65);
  color:rgba(10,31,68,.98);
}
.btn--primary:hover{ filter:brightness(.98); }
.btn--secondary{
  background:rgba(10,31,68,.92);
  border-color:rgba(10,31,68,.85);
  color:var(--white);
}
.btn--ghost{
  background:transparent;
  border-color:rgba(255,255,255,.28);
  color:var(--white);
}
.section .btn--ghost{
  border-color:rgba(10,31,68,.22);
  color:var(--navy);
}
.center{ margin-top:18px; text-align:center; }

/* Footer */
.footer{
  border-top:1px solid var(--line);
  padding:22px 0;
  background:var(--white);
}
.footer__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  flex-wrap:wrap;
}
.footer__links{ display:flex; gap:14px; }
.to-top{ color:rgba(10,31,68,.85); }

/* Modal */
.modal{
  position:fixed;
  inset:0;
  display:none;
  z-index:80;
}
.modal.is-open{ display:block; }
.modal__backdrop{
  position:absolute; inset:0;
  background:rgba(10,31,68,.55);
}
.modal__panel{
  position:relative;
  width:min(560px, calc(100% - 30px));
  margin:10vh auto 0;
  background:var(--white);
  border-radius:22px;
  padding:18px;
  box-shadow: 0 20px 60px rgba(10,31,68,.25);
  border:1px solid rgba(255,255,255,.35);
}
.modal__head{
  display:flex; align-items:center; justify-content:space-between;
  gap:10px;
}
.icon-btn{
  border:1px solid rgba(10,31,68,.18);
  background:transparent;
  border-radius:12px;
  padding:8px 10px;
  cursor:pointer;
}
.modal__actions{ display:flex; gap:10px; margin-top:12px; flex-wrap:wrap; }

/* Animations */
.fade-in{
  opacity:0;
  transform: translateY(10px);
  transition: opacity .6s ease, transform .6s ease;
}
.fade-in.is-visible{
  opacity:1;
  transform: translateY(0);
}

/* Responsive */
@media (max-width: 980px){
  .grid-3, .pricing, .team{ grid-template-columns: 1fr; }
  .contact{ grid-template-columns: 1fr; }
}
@media (max-width: 860px){
  .nav{ display:none; }
  .burger{ display:block; }
  .mobile-nav.is-open{ display:block; }
}
